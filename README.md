# PyPKG Manager
# "Giga version" pending (an extensively revised version employing many new techniques).

Yes, it's a new PKG Manager, and this one is made in PYTHON, THE beginner language.

I didn't know how to code before this project and I still don't know how to code. I've always wanted to make a python project like this one,

So if you want to judge me because the code absolutely reeks of shit, that's completely OK. Otherwise, see ya, and enjoy this project that I'll probably abandon in about a week.

The site to download the newer version: <a href="https://pypkg-manager.vercel.app/">PyPKG</a>.
You also need to run '-up' to update the package manager.

---
### **How do I launch it?**
You need to have Python and install the `requests` library. 

For Windows: `pip install requests`

For Ubuntu / Debian / Mint `sudo apt update && sudo apt install python3-requests`

For Fedora `sudo dnf install python3-requests`

For Arch Linux / Manjaro  `pacman -S python-requests`

For MacOS `pip3 install requests`

And launch it using the command in the directory: `python main.py`

OR
In this new version, the .zip file contains an "Installer" folder; inside, you'll find .sh and .bat installers that you can simply run. It should cover all major operating systems (Arch, Debian, Fedora, Windows).
